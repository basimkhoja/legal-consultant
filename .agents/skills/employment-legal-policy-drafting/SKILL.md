---
name: employment-legal-policy-drafting
description: "Draft an employment policy or work-regulations section with jurisdiction supplements where the law differs across the footprint (by state for `usa`; by nationality, sector, gender-specific rules or entity type for a populated code). Use when the user says 'draft a [topic] policy', 'we need a policy on', 'update our [topic] policy', or names a policy gap."
argument-hint: "[policy topic — e.g., 'remote work', 'parental leave', 'PTO']"
---

> **Runtime note (generated by `scripts/build-runtimes.py`; do not edit this copy).** You are running the `policy-drafting` skill of the `employment-legal` plugin under OpenAI Codex CLI. Read the practice profile at `${LEGAL_CONSULTANT_HOME:-~/.legal-consultant}/employment-legal/CLAUDE.md`; if it is missing, run the `employment-legal-cold-start-interview` skill first. Every path in this skill of the form `references/jurisdictions/<code>/…` resolves to .agents/skills/legal-consultant-jurisdictions/references/jurisdictions/. The shared guardrails that the Claude Code plugin keeps in its `CLAUDE.md` are in AGENTS.md at the repo root (or your global AGENTS.md) and apply to every step below. Paths of the form `~/.claude/plugins/config/claude-for-legal/<plugin>/…` in upstream-inherited text mean `${LEGAL_CONSULTANT_HOME:-~/.legal-consultant}/<plugin>/…`.

# /policy-drafting

1. Load `${LEGAL_CONSULTANT_HOME:-~/.legal-consultant}/employment-legal/CLAUDE.md` → jurisdictional footprint, handbook location.
2. Use the workflow below.
3. Draft core policy. Check each jurisdiction in footprint for required variants.
4. Output: core policy + jurisdiction supplements (axis per the applicable code). Flag where law is currently shifting.

---

## Matter context

**Matter context.** Check `## Matter workspaces` in the practice-level CLAUDE.md. If `Enabled` is `✗` (the default for in-house users), skip the rest of this paragraph — skills use practice-level context and the matter machinery is invisible. If enabled and there is no active matter, ask: "Which matter is this for? Run `/employment-legal:matter-workspace switch <slug>` or say `practice-level`." Load the active matter's `matter.md` for matter-specific context and overrides. Write outputs to the matter folder at `${LEGAL_CONSULTANT_HOME:-~/.legal-consultant}/employment-legal/matters/<matter-slug>/`. Never read another matter's files unless `Cross-matter context` is `on`.

---

## Purpose

A policy that's right for one jurisdiction may be wrong (or unnecessary) in the next (for `usa`, California against Texas). This skill drafts a core policy and generates jurisdiction supplements where the footprint requires different rules. The supplement **axis** depends on the code: for `usa` the axis is the state; for a populated code it is whatever the jurisdiction files vary the rule on — for `ksa`: nationality (national / non-national), sector or activity, gender-specific rules, and entity type — and the policy must sit inside the file's binding template where one exists.

## Load context

`${LEGAL_CONSULTANT_HOME:-~/.legal-consultant}/employment-legal/CLAUDE.md` → jurisdictional footprint, handbook location and format, `## Handbook` (jurisdiction supplements, regulatory approval status, language versions).

## Workflow

### Step 0: Resolve the applicable jurisdiction

1. **Read the practice profile's `## Jurisdiction` section.** It gives the primary jurisdiction code, the footprint list (other codes the practice operates in), the output-language preference, and whether local counsel is available for escalation. Codes are ISO 3166-1 alpha-3 lowercase (`ksa`, `gbr`, `fra`, `che`, `usa`). If the section is missing or still a placeholder, stop: "The practice profile has no jurisdiction. Run the cold-start interview; nothing in this skill can run against the wrong jurisdiction."
2. **Determine the matter's jurisdiction(s).** Start from the primary code. Then read the matter facts: governing-law clause, seat of arbitration, place of employment, jurisdiction of incorporation, place of performance. If the facts point to a code not in the profile, add it for this matter and say so in the reviewer note. A matter may have more than one code (a contract governed by English law with a Saudi counterparty and Saudi performance is `gbr` + `ksa`).
3. **Load the jurisdiction folder for each code.** The folder is `references/jurisdictions/<code>/` in this plugin (the same tree ships at the repo root and in every runtime adapter). Read `MANIFEST.md` first.
   - If `populated` is not `yes`: **stop for that code.** Say: "Jurisdiction `<code>` (<name>) is registered but not populated: no reference files exist for it. I will not apply another jurisdiction's rules or model knowledge in its place. Options: (1) populate `references/jurisdictions/<code>/` (see README, 'How to add a jurisdiction'), (2) route this matter to local counsel, (3) tell me to proceed with the analysis limited to the populated jurisdictions in this matter, with every finding for `<code>` marked `[not populated — no rule applied]`." Wait for the answer. Never fall back silently.
   - If the code is `usa`: there is no folder. Follow this skill's US path (the upstream doctrine and the upstream research connectors, CourtListener or Westlaw, with the upstream "no silent supplement" rule). Label findings `[usa]`.
   - If `populated` is `yes`: read `INDEX.md`, then the instrument files this skill names in its "Jurisdiction files" list. A row tagged `[settled — last confirmed YYYY-MM-DD]` may be applied and cited by article. A row tagged `[model knowledge — verify]` may be applied only with that tag carried onto the finding. If a rule this skill needs is not in the files at all, do not supply it from memory: say what is missing, tag the gap `[no rule in <code> files — verify]`, and continue only with the rules that exist.
4. **Multi-jurisdiction matters.** Run the relevant files side by side. Label every finding with its code in square brackets, `[ksa]`, `[gbr]`, `[usa]`, and never merge two jurisdictions' rules into one sentence. Where the codes conflict (a clause valid under one law and reducible under another), state both and flag `[review]` for the lawyer to decide which governs.
5. **Research step (when a rule must be quoted or its currency checked).** Use the portal named in `MANIFEST.md` → `research_tool`. For `ksa`: `python3 scripts/fetch-law.py --portal boe --id <guid> --lang ar` (GUIDs in `SOURCES.md`), or `curl -sS https://laws.boe.gov.sa/BoeLaws/Laws/LawDetails/<guid>/1`; the built-in web-fetch tool rejects the portal's TLS chain. Quote the article, tag `[BOE — Arabic]` (or `[BOE — official English]` when quoting the translation), and check the status line and the "تعديلات المادة" block for amendments. If the fetch fails or the article is not found, apply the "no silent supplement" rule: report the failure and stop, or continue with the rule tagged `[model knowledge — verify]` only if the user says so.
6. **Calendar and language.** Take the weekend, public-holiday, calendar (Hijri or Gregorian) and currency rules from `MANIFEST.md`. Compute every date and roll-back against that calendar, never against a Saturday/Sunday weekend or US federal holidays. Produce the deliverable in English; when the profile's output-language preference is bilingual, or the manifest's authoritative language is not English and counterparty-facing text is produced, add the authoritative-language rendering of the bottom line, the findings table, and any counterparty-facing text, using the spellings in the manifest's `output_language_rule`.
7. **Header and disclaimer.** Prepend the manifest's `disclaimer` line under the work-product header for every deliverable that applies a non-`usa` jurisdiction. For `ksa`: "Arabic text is authoritative; English translations are for convenience; a licensed Saudi lawyer must review before reliance." with its Arabic rendering from the manifest.
8. **Record in the reviewer note.** `Jurisdiction: <codes applied>; files: <list>; portal fetched: yes/no; unpopulated codes: <list or none>.`

**Jurisdiction files this skill loads** (for a populated non-`usa` code; for `ksa` these are the files named, for another populated code the equivalent instrument files listed in that folder's `INDEX.md`):

- `references/jurisdictions/<code>/MANIFEST.md` (calendar, language, disclaimer), `INDEX.md`, `playbook-defaults.md` Labor table (work regulations, working time, leave rows).
- `references/jurisdictions/<code>/labor-law.md` — Art. 8 (the Law is a floor; contrary clauses void), Arts. 9-10 (Arabic text governs; calendar), Arts. 12-13 (model work regulations; every employer must prepare and publish regulations in line with the model), Art. 61(4)-(6) (non-discrimination; housing and transport), Arts. 66-73 (closed penalty list; a penalty must be in the Law or the work regulations), Art. 83 (non-compete), Arts. 98-108 (working time, Ramadan, rest, overtime, exempt categories), Arts. 109-118 (leave), Arts. 121-125 (safety duties), Arts. 151-160 (women's provisions: maternity, nursing, pregnancy protection, nursery, iddah), Arts. 84-88 (end-of-service). Steps 2, 3, 5.
- `references/jurisdictions/<code>/labor-law-implementing-regulations.md` — Reg. Art. 3 (Annex 1 model work regulations are the approved model), Reg. Art. 4(1) (certification by an accredited law firm and submission; electronic adoption of the unmodified model for establishments under 50 workers), Reg. Art. 4(2) (conflicting clauses of earlier regulations void), Annex 1 Arts. 33-44 (leave), 47-49 (safety, insurance, registration), 50-51 (maternity, nursing), 53-56 (conduct and harassment), 57 (termination grounds), 58-71 (discipline, violations schedule, grievance), Reg. Art. 22 bis (compensatory leave), Reg. Arts. 24-26 (holidays, unpaid and sick leave). Steps 2, 3, 4, 5.
- `references/jurisdictions/<code>/occupational-safety.md` — Arts. 122-125, 142-143, Reg. Art. 29, Annex 1 Arts. 47-49 (HSE, PPE, incident-reporting, fitness-for-duty sections). Step 2.
- `references/jurisdictions/<code>/labor-dispute-route.md` — Annex 5 cls. 13.3-13.4 and 14.5, Art. 8 (dispute clauses). Step 2.
- `references/jurisdictions/<code>/saudization-nitaqat.md` and `platform-obligations.md` — only when the policy touches recruitment, transfers, wage payment or permits. Step 2.

### Step 1: Scope the policy

- What's the policy for? (Remote work, parental leave, social media, etc.)
- Why now? (Legal requirement, incident, growth, gap noticed)
- Who does it apply to? (All employees, certain roles, certain locations)

### Step 2: Jurisdictional scan

For each jurisdiction code (and sub-jurisdiction or segment) in the footprint, check: does this jurisdiction have a specific rule on this topic?

**When the applicable code is `usa`** — common topics with jurisdictional variance (axis: state):

| Topic | Variance |
|---|---|
| Paid leave | State mandates (CA, NY, CO, WA, etc.) with different accrual rates, uses, carryover |
| Parental leave | State programs layer on top of FMLA (CA PFL, NY PFL, etc.) |
| Meal and rest breaks | CA is the outlier (penalty pay); most states minimal |
| Expense reimbursement | CA requires; most states don't |
| Pay transparency | Growing list of states requiring ranges in postings |
| Non-competes | See hiring-review skill — unenforceable in some states |
| Final pay | Timing varies widely |

**When the applicable code is populated (non-`usa`)** — the topic table comes from the jurisdiction files; each cell is a pointer to a row, applied with the row's tag. For `ksa` (axis: nationality, sector, gender-specific rules, entity type; the Law itself is the same across the territory):

| Topic | Variance axis | Source rows |
|---|---|---|
| Working time and Ramadan hours | Religion of the worker (Ramadan reduction for Muslim workers); category (senior managers, intermittent work, guards and cleaners); shift establishments | `labor-law.md` Arts. 98-101, 106-108; `labor-law-implementing-regulations.md` Reg. Arts. 22, 22 bis, 23 |
| Leave | Length of service (annual leave 21 → 30 days after five years); Hajj eligibility (two years' service, once); exam leave (approved enrolment); public holidays and overlaps | `labor-law.md` Arts. 109-117; `labor-law-implementing-regulations.md` Annex 1 Arts. 33-44, Reg. Arts. 24-26 |
| Women's provisions | Gender-specific rules: maternity 12 weeks at full pay, nursing hour for 24 months, no dismissal during pregnancy or maternity leave, seats for rest, nursery at 50 or more female workers, iddah leave; hazardous-work limits only through the Art. 131 bis list (not read — tag) | `labor-law.md` Arts. 151-160, 131 bis; `labor-law-implementing-regulations.md` Annex 1 Arts. 50-51, Reg. Art. 31 |
| End-of-service | Mode of ending (resignation fractions), service length, wage components; non-national contracts fixed-term | `labor-law.md` Arts. 84-88, 37; Annex 5 cls. 11.9-11.10 |
| Contract type, probation, notice | Nationality (Art. 37: non-national fixed-term); contract type (notice only on indefinite contracts) | `labor-law.md` Arts. 37, 53-55, 75-77 |
| Discipline and grievance | None by segment: closed penalty list, violations schedule, 30-day clocks, 30-15-30 grievance ladder apply to every worker | `labor-law.md` Arts. 66-73; Annex 1 Arts. 58-71 |
| Conduct, dress code, harassment | Model regulations' mandatory sections; committee procedure | Annex 1 Arts. 53-56 |
| Housing, transport, remote sites | Entity type and location (remote-site duties); allowances for every worker | `labor-law.md` Arts. 61(5)-(6), 146-148; `occupational-safety.md` Reg. Art. 30 |
| Non-compete and confidentiality | Role (client knowledge, secrets) | `labor-law.md` Art. 83 |
| Wage payment | Pay frequency by wage type; bank payment for all | `labor-law.md` Art. 90; `platform-obligations.md` Art. 90 row (WPS row `[model knowledge — verify]`) |
| Work-regulations model, certification and approval route, Arabic version | Establishment size (under 50 workers may adopt the unmodified model electronically; otherwise certification by an accredited law firm); language (Arabic governs) | `labor-law-implementing-regulations.md` Reg. Arts. 3, 4(1), 4(2); `labor-law.md` Arts. 9, 12-13 |

For another populated code, build the table from that code's files. **Row missing:** if the files carry no rule on the policy topic (for `ksa`: remote work — the regulations file records it has no remote-work article; pay transparency; expense reimbursement), say so, tag `[no rule in <code> files — verify]`, and draft the section as a company rule that must not go below the Art. 8 floor.

**Research step (populated codes).** To quote an article or check its currency, fetch the instrument from the portal named in the manifest (`scripts/fetch-law.py --portal boe --id <guid> --lang ar`, GUIDs in `references/jurisdictions/<code>/SOURCES.md`; the built-in web-fetch tool rejects the portal's TLS chain — use the script or `curl`), quote it, tag `[BOE — Arabic]` or `[BOE — official English]`. If the fetch fails, report it and stop, or continue on the file row with its own tag only if the user says so. For `usa`, the upstream research connectors.

If the topic has no jurisdictional variance (dress code, say — though for `ksa` the dress-code section is itself a mandatory model clause, Annex 1 Art. 53), skip this step.

### Step 3: Draft the core policy

One policy. Applies everywhere. Clear and readable — employees should understand it without a lawyer.

Structure:
- Purpose (one sentence — why this policy exists)
- Scope (who it applies to)
- The rule (what's required/permitted/prohibited)
- Process (how to request, who approves, what happens if)
- Questions (who to ask)

Avoid: "heretofore," "notwithstanding," nested exceptions. **When the applicable code is `usa`:** this is a handbook policy, not a contract. **When the applicable code is populated (non-`usa`):** for `ksa` the policy is a section of the employer's work regulations, which are binding once certified or approved and published (`labor-law.md` Arts. 12-13, Art. 67; `labor-law-implementing-regulations.md` Reg. Arts. 3-4); draft it to slot into the Annex 1 model article it modifies, in Arabic with an English convenience version (Art. 9), and never below the Law's floor (Art. 8).

### Step 4: Jurisdiction supplements

For each jurisdiction (or segment on the applicable axis) where the rule differs, a supplement:

```markdown
### [Jurisdiction / segment] Supplement

Employees [working in [State] | in segment [nationality / sector / gender-specific rule / entity type]] are subject to the following in addition to / instead of the core policy:

- [Specific difference]
- [Cite the state law (`usa`) or the jurisdiction-file row with its tag (populated code)]
```

Keep supplements tight. Only what's different — don't repeat the core. For a populated code, a supplement may not carve a segment below the file's floor (for `ksa`, Art. 8); a national/non-national split is lawful only where the file itself splits (for `ksa`: contract term under Art. 37, flexible work under Reg. Art. 27, permit and exit rules), not on pay or leave.

### Step 5: Cross-check

- Does this policy conflict with anything already in the handbook?
- Does it promise more than the company intends to deliver? (A policy is a promise — courts hold employers to handbook promises.)
- Does it inadvertently create a contract? **`usa` only:** some states treat handbook policies as contractual — include the standard "this is not a contract" language if the handbook doesn't already. **Populated codes:** do not add that language; for `ksa` the work regulations are binding on both sides once certified or approved per `labor-law-implementing-regulations.md` Reg. Art. 4 and `labor-law.md` Art. 13, a policy that is not in the approved regulations is not enforceable as a disciplinary instrument (Art. 67 via the Arts. 12-13 row), and any clause that conflicts with the Law, the Regulations or the model is void (Reg. Art. 4(2), Art. 8).
- Populated codes: does the draft add a penalty outside the closed list (for `ksa`, Arts. 66-67 and Annex 1 Art. 59), waive a statutory right (Art. 8), or contradict a mandatory model section (Annex 1 Arts. 53-56 conduct, 58-71 discipline)?

## Output

```markdown
# [Policy Name]

## Core Policy

[Full text]

## Jurisdiction Supplements

### [Jurisdiction / segment 1]
[Supplement]

### [Jurisdiction / segment 2]
[Supplement]

---

## Drafting Notes (internal — remove before handbook insertion)

- **Jurisdiction:** [codes applied; files: <list>; portal fetched: yes/no; unpopulated codes: <list or none>]
- **Jurisdictional scan:** [which jurisdictions or segments checked, which have variance, each with its source row and tag]
- **Conflicts with existing handbook:** [none | list]
- **Law currently shifting:** [any jurisdiction where this is in flux; for a populated code, rows tagged `[model knowledge — verify]` and the amendment-watch leads]
- **Regulatory step (populated codes):** [for `ksa`: which Annex 1 article this modifies; certification route under Reg. Art. 4(1) (accredited law firm, or electronic adoption of the unmodified model for under 50 workers); Arabic version required (Art. 9); publication to workers (Art. 13)]
- **Review cadence:** [when to revisit — annual, or when X happens]
```

> **Header, disclaimer and language.** Prepend the work-product header to the drafting notes, then — for every code other than `usa` — the jurisdiction disclaimer line from the profile (`## Outputs`). The policy text itself is counterparty-facing: apply the bilingual house style in `## Outputs` (for `ksa`, the Arabic version is the governing text under `labor-law.md` Art. 9 and the English is a convenience rendering) and use the spellings in the manifest's `output_language_rule`. Money in `[currency]`; dates and holidays on the manifest's calendar.

> **Draft, not a policy in effect.** This is a drafting aid for attorney review, not a policy you can publish. Publishing a handbook policy has legal consequences — in several jurisdictions it can bind the company as a contractual promise (and for a populated code such as `ksa` the certified work regulations are binding by statute), and wage/leave/accommodation policies are routinely read against the employer. A licensed attorney, solicitor, barrister, or other authorised legal professional in your jurisdiction reviews, edits as needed, and takes professional responsibility before the policy is rolled out. Do not publish or distribute this draft unreviewed.

## Handoff

To handbook-updates skill: when this policy is approved, it diffs against the current handbook and flags what changes.

## What this skill does not do

- Approve the policy. It drafts; a human approves.
- Roll out the policy. Communication to employees is an HR workflow.
- Cover every jurisdiction on earth — only the ones in the footprint. If the footprint expands, re-run.
- Apply another jurisdiction's rules to an unpopulated code, or invent a variance the jurisdiction file does not carry.
