description = "Read VDR documents and extract issues per house categories and materiality thresholds, producing findings in house memo format. Use when user says 'review the data room', 'extract issues from [folder]"
prompt = """
Activate the `corporate-legal-diligence-issue-extraction` skill bundled in this extension and run it as written, using the practice profile at ${LEGAL_CONSULTANT_HOME:-~/.legal-consultant}/corporate-legal/CLAUDE.md. User input: {{args}}
"""
