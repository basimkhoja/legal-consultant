description = "Draft a written consent or resolution by circulation of the board or a committee in house format, with precedent search from the consents repository. Handles multi-resolution consents, director confli"
prompt = """
Activate the `corporate-legal-written-consent` skill bundled in this extension and run it as written, using the practice profile at ${LEGAL_CONSULTANT_HOME:-~/.legal-consultant}/corporate-legal/CLAUDE.md. User input: {{args}}
"""
